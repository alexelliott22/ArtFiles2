{{!-- <ol class="post-list">
    <li>
        <article class="artwork">
            <div class="title">
                <a href="{{}}" target="_blank">{{posts[0].title}}</a>
                <span>({{post_url}})</span>
            </div>
            <div class="meta">
                {{vote_count}} point(s) by {{user.username}} on
                {{created_at}}
                |
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
            </div>
        </article>
    </li>
</ol> --}}

<div>
    {{#each posts as |post|}}
        <h2>{{post.title}}</h2>
        <h4>{{post.medium}}</h4>
    {{/each}}
</div>